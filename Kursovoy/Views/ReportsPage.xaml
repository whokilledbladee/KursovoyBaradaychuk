<UserControl x:Class="Kursovoy.Views.ReportsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="📋 Сохраненные отчеты" 
                      FontSize="18" 
                      FontWeight="Bold" 
                      Foreground="#2C3E50"
                      Margin="0,0,0,10"/>
            <TextBlock Text="Отчеты, сформированные во вкладке 'Диаграммы'" 
                      FontSize="12" 
                      Foreground="#7F8C8D"
                      Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="🔄 Обновить" 
                       Click="RefreshButton_Click"
                       Style="{StaticResource ActionButtonStyle}"
                       Margin="0,0,10,0"/>
                <Button Content="🗑️ Удалить выбранный" 
                       Click="DeleteReportButton_Click"
                       Style="{StaticResource ActionButtonStyle}"/>
            </StackPanel>
        </StackPanel>

        <ListBox Grid.Row="1" 
                x:Name="ReportsListBox" 
                BorderThickness="0" 
                Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#BDC3C7" 
                           BorderThickness="1" 
                           CornerRadius="5" 
                           Margin="0,0,0,10" 
                           Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" 
                                       Orientation="Horizontal" 
                                       Margin="0,0,0,10">
                                <TextBlock Text="{Binding Title}" 
                                          FontWeight="Bold" 
                                          FontSize="16" 
                                          Foreground="#2C3E50"/>
                                <TextBlock Text="{Binding DisplayDate}" 
                                          Foreground="#7F8C8D" 
                                          Margin="10,0,0,0"
                                          VerticalAlignment="Center"/>
                            </StackPanel>

                            <Grid Grid.Row="1" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Доходы" 
                                              FontSize="11" 
                                              Foreground="#27AE60"/>
                                    <TextBlock Text="{Binding TotalIncome, StringFormat={}{0:N0} р.}" 
                                              FontWeight="Bold"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Расходы" 
                                              FontSize="11" 
                                              Foreground="#E74C3C"/>
                                    <TextBlock Text="{Binding TotalExpenses, StringFormat={}{0:N0} р.}" 
                                              FontWeight="Bold"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Баланс" 
                                              FontSize="11" 
                                              Foreground="#3498DB"/>
                                    <TextBlock Text="{Binding Balance, StringFormat={}{0:N0} р.}" 
                                              FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Grid.Row="2" Orientation="Horizontal">
                                <TextBlock Text="{Binding IncomeData.Count, StringFormat={}Категорий доходов: {0}}" 
                                          Foreground="#7F8C8D"
                                          Margin="0,0,20,0"/>
                                <TextBlock Text="{Binding ExpenseData.Count, StringFormat={}Категорий расходов: {0}}" 
                                          Foreground="#7F8C8D"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>