<Window x:Class="Kursovoy.Views.GoalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить цель" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" 
                  Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameTextBox" 
                Margin="0,0,0,10" Height="25"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Целевая сумма:" 
                  Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TargetAmountTextBox" 
                Margin="0,0,0,10" Height="25"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Текущая сумма:" 
                  Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="CurrentAmountTextBox" 
                Margin="0,0,0,10" Height="25"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" 
                   Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="SaveButton_Click" 
                   Width="80" Margin="0,0,10,0" Style="{StaticResource ActionButtonStyle}"/>
            <Button Content="Отмена" Click="CancelButton_Click" 
                   Width="80" Style="{StaticResource ActionButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>