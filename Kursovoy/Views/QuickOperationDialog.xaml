<Window x:Class="Kursovoy.Views.QuickOperationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Быстрая операция" Height="250" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding AccountInfo}" 
                  FontWeight="SemiBold" Margin="0,0,0,10" TextAlignment="Center"/>

        <TextBlock Grid.Row="1" Text="Сумма:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="AmountTextBox" 
                Height="30" FontSize="14"
                Margin="0,0,0,15"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="ОК" Click="OkButton_Click" 
                   Width="80" Margin="0,0,10,0" Style="{StaticResource ActionButtonStyle}"/>
            <Button Content="Отмена" Click="CancelButton_Click" 
                   Width="80" Style="{StaticResource ActionButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>