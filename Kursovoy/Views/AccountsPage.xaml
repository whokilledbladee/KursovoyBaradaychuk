<UserControl x:Class="Kursovoy.Views.AccountsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="➕ Добавить счет" 
                    Click="AddAccount_Click" 
                    Style="{StaticResource ActionButtonStyle}"
                    Margin="0,0,10,0"/>
            <Button Content="✏️ Редактировать" 
                    Click="EditAccount_Click"
                    Style="{StaticResource ActionButtonStyle}"
                    Margin="0,0,10,0"/>
            <Button Content="❌ Удалить" 
                    Click="DeleteAccount_Click"
                    Style="{StaticResource ActionButtonStyle}"/>
        </StackPanel>
        <ListBox Grid.Row="1" x:Name="AccountsListBox" 
                BorderThickness="0" 
                Background="Transparent"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="280" Height="180" 
                            Background="White" 
                            BorderBrush="#BDC3C7" 
                            BorderThickness="1" 
                            CornerRadius="15"
                            Margin="10">

                        <Grid Margin="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Name}" 
                                          FontWeight="SemiBold" 
                                          Foreground="#2C3E50"
                                          VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center">
                                <TextBlock Text="БАЛАНС" 
                                          FontSize="11" 
                                          Foreground="#7F8C8D" 
                                          HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Balance, StringFormat={}{0:# ##0} р.}" 
                                          FontSize="24" 
                                          FontWeight="Bold" 
                                          Foreground="#2C3E50"
                                          HorizontalAlignment="Center"
                                          Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding TypeName}" 
                                          FontSize="11" 
                                          Foreground="#7F8C8D" 
                                          HorizontalAlignment="Center"
                                          Margin="0,5,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="+" 
                                       Click="QuickIncome_Click"
                                       Tag="{Binding}"
                                       Style="{StaticResource QuickActionButtonStyle}"
                                       Margin="0,0,10,0"/>
                                <Button Content="-" 
                                       Click="QuickExpense_Click"
                                       Tag="{Binding}"
                                       Style="{StaticResource QuickActionButtonStyle}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>